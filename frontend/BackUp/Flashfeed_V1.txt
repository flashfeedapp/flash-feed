<div [class.dark]="showDarkMode" class="home-container">
    <header class="header">
        <div class="title">Flash Feed</div>
        <button class="toggle-theme" (click)="toggleTheme()">Toggle</button>
    </header>

    <div class="tabs">
        <div *ngFor="let tab of tabs" class="tab" [class.active]="tab === selectedTab" (click)="selectTab(tab)">{{tab}}
        </div>
        <div class="tab more">More</div>
    </div>

    <div class="news-list">
        <div class="news-card" *ngFor="let news of newsList">
            <img [src]="'images/rain.jpg'" alt="Rain" class="news-image">
            <div class="news-title">{{news.title}}</div>
            <div class="news-summary">{{news.summary}}</div>
            <div class="card-actions">
                <div class="left-info">
                    <span class="news-source"> {{news.source}}</span>
                    <span class="dot">.</span>
                    <span class="news.time">{{news.time}}</span>
                </div>
                <div class="right-buttons">
                    <button class="icon-btn">Share</button>
                    <button class="icon-btn">Comment</button>
                </div>
            </div>
        </div>
        <div *ngIf="newsList.length === 0" class="no-data">
            No news available fro {{selectedTab}}
        </div>
    </div>

</div>


-------------------------------------------------------------------------------------------------


.dark {
    --bg: #1f2430;
    --card: #2a2f44;
    --text: #e5e7eb;
    --muted: #9ca3af;
}

.home-container {
    background: var(--bg);
    padding: 16px;
    min-height: 100vh;
    color: var(--text);
    font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
}

.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 14px;
}

.toggle-theme {
    background: transparent;
    border: none;
    font-size: 20px;
    cursor: pointer;
    color: var(--muted);
}

.tabs {
    display: flex;
    gap: 8px;
    overflow-x: auto;
    margin-bottom: 16px;
}

.tab {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 8px 14px;
    background: var(--card);
    border: 1px solid rgba(0,0,0,1.05);
    border-radius: 999px;
    font-size: 13px;
    cursor: pointer;
    flex-shrink: 0;
    transition: all;
    color: var(--text);
}

.tab.active {
    color: var(--text);
    border: 2px solid #FF5252;
}

.news-list {
    display: flex;
    flex-direction: column;
    gap: 14px;
}

.news-card {
    background: var(--card);
    padding: 14px;
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    position: relative;
    overflow: hidden;
}

.news-image {
    width: 100%;
    height: 180px;
    object-fit: cover;
}

.news-title {
    font-size: 18px;
    font-weight: bold;
    padding: 10px;
    display: flex;
    align-items: center;
}

.news-summary {
    font-size: 14px;
    color: var(--muted);
    padding: 0 10px 10px 10px;
    line-height: 1.5;
}

.card-actions {
    display: flex;
    justify-content: space-between;
    padding: 8px 10px;
}

.dot {
    margin: 0 4px;
}

.news-source {
    text-align: left;
}

.left-info {
    display: flex;
    align-items: center;
    gap: 4px;
    color: var(--muted);
}

.right-buttons {
    display: flex;
    gap: 8px;
}

--------------------------------------------------------------------------------------------------------



import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { NewsService } from '../services/news.service';
import { NewsItem } from '../core/models/news.model';

@Component({
  selector: 'app-home',
  imports: [CommonModule],
  templateUrl: './home.html',
  styleUrl: './home.scss'
})
export class Home {
  showDarkMode = false;
  tabs: string[] = [];
  selectedTab: string = '';
  newsList : NewsItem[] = [];

  constructor(private newsService: NewsService) {}

  ngOnInit(): void {
    this.tabs = ["Local", "State", "National", "International"];
    this.selectedTab = this.tabs[0];
    this.loadNews(this.selectedTab);
  }

  loadNews(tab: string) {
    this.newsService.getNews(tab).subscribe({
      next : (data) => {
        this.newsList = data;
      }
    })
    console.log(this.newsList);
  }

  toggleTheme() {
    this.showDarkMode = !this.showDarkMode;
  }

  selectTab(tab: string) {
    if(this.selectedTab == tab) {
      return;
    }
    this.selectedTab = tab;
    this.loadNews(this.selectedTab);
  }
}
